// Title: Count Rotations With Exactly K Equal Adjacent Pairs
            // Difficulty: Easy
            // Language: C++
            // Link: https://leetcode.com/problems/count-rotations-with-exactly-k-equal-adjacent-pairs/

    int countRotations(string s, int k) {
        int n = s.size();
        int ans = 0;
    }
        return ans == k;
        }
        for(int i = 0;i<n;i++){
            if(ans > k) return 0;
            if(s[i] == s[i+1]) ans++;
        for(int i = 0;i<s.size()-1;i++){
        int ans = 0;
    bool calc_score(string & s,int k){
public:
class Solution {
